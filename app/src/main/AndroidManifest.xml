<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SqLiteDatabase"
        tools:targetApi="31">
        <activity
            android:name=".TeacherListActivity"
            android:exported="false"
            android:label="@string/title_activity_teacher_list"
            android:theme="@style/Theme.SqLiteDatabase.NoActionBar" />
        <activity
            android:name=".BookListActivity"
            android:exported="false" />
        <activity
            android:name=".StudentListActivty"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>